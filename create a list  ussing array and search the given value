#include <stdio.h>

void search(int*arr,int n){
    int value;
    scanf("%d",&value);
    for(int i=0;i<n;i++){
        if(arr[i]==value){
            printf("Found");
        
            
        }
        else
        {
            printf("Not Found");
        }
    }
}
int main(){
    int n;
    scanf ("%d", &n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    search(arr,n);
    return 0;
}
